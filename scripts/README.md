# Analysis scripts
Place the analysis scripts that produce the published tables here, so data,
code, and site travel together and the Zenodo snapshot captures everything:
- order_channel_migration.py (produces order_channel_share_by_fy.csv)
- band_rule_of_two_exposure.py (produces the exposure layer table)
Each script prints reconciliation checks; publication requires all checks to pass.
